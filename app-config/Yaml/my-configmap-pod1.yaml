apiVersion: v1
kind: Pod
metadata:
  name: my-configmap-pod1-1
spec:
  containers:
    - name: nginx
      image: nginx
      ports:
      - containerPort: 80
      envFrom:
        - configMapRef:
            name: my-configmap
            name: app.configfile